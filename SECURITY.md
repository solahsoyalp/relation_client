# セキュリティポリシー

## 脆弱性の報告

セキュリティ上の問題を発見した場合は、公開 Issue ではなく、リポジトリオーナー宛に
非公開で連絡してください。

## サポート対象 Python

**Python 3.10 以上**をサポートします（3.8 / 3.9 は v0.3.0 でサポート終了）。

これは依存パッケージの既知脆弱性修正版が Python 3.10 以上を要求するためです
（下記）。3.8 / 3.9 を利用する場合は、脆弱性修正を受け取れない最終対応版
`relation-client 0.2.x` を利用してください。

## 依存パッケージの方針

既知脆弱性の修正版を直接依存として要求します。いずれも修正版が Python 3.10 以上を
必須とするため、本プロジェクトのサポート下限（3.10）と整合しています。

| パッケージ | 要求バージョン | 回避する既知脆弱性 |
| --- | --- | --- |
| `requests` | `>= 2.33.0` | CVE-2023-32681 / CVE-2024-35195 / CVE-2026-25645 |
| `urllib3` | `>= 2.7.0` | CVE-2026-44431 / CVE-2026-44432 |

`urllib3` は `requests` の推移的依存ですが、`requests` の指定（`urllib3>=1.21.1,<3`）が
緩いため、修正版を確実に選択させる目的で直接依存として下限を補強しています。

## 依存監査

CI で [`pip-audit`](https://github.com/pypa/pip-audit) を実行し、依存パッケージの
既知脆弱性を継続的に確認しています（`.github/workflows/ci.yml` の `audit` ジョブ）。
ローカルでも以下で実行できます。

```bash
python -m pip install pip-audit
pip-audit -r relation_client/requirements.txt
```
